cmake_minimum_required(VERSION 3.8)
project(warehouse_robot_navigation)

find_package(ament_cmake REQUIRED)

install(DIRECTORY urdf meshes worlds config maps launch rviz
  DESTINATION share/${PROJECT_NAME}
  OPTIONAL
)

ament_package()
